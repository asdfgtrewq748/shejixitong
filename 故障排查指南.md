# 上传文件无反应 - 故障排查指南

## 问题描述
- favicon.ico 404错误（不影响功能，可忽略）
- 上传CSV文件没有反应

## 排查步骤

### 1. 检查后端服务是否启动 ⭐⭐⭐

```powershell
# 打开新终端，进入backend目录
cd backend

# 启动后端服务
npm start
```

**预期输出**：
```
🚀 后端服务已启动: http://localhost:3001
```

**如果没有输出或报错**：
```powershell
# 安装依赖
npm install

# 再次启动
npm start
```

### 2. 检查前端是否连接到正确端口 ⭐⭐

打开浏览器开发者工具（F12）-> Console标签页

查看是否有以下错误：
- `Failed to fetch` - 后端未启动
- `CORS error` - 跨域问题
- `Network error` - 端口错误

### 3. 测试后端API是否正常 ⭐

在浏览器中打开：
```
http://localhost:3001/api/health
```

**预期返回**：
```json
{"status":"ok","timestamp":1733097600000}
```

如果无法访问，说明后端未启动。

### 4. 检查上传目录权限

```powershell
# 确保uploads目录存在
cd backend
mkdir uploads -Force
```

### 5. 查看浏览器控制台错误信息 ⭐⭐⭐

1. 打开浏览器 F12
2. 进入 **Network** 标签页
3. 点击上传按钮
4. 查看是否有红色的失败请求

**常见错误**：

| 错误信息 | 原因 | 解决方法 |
|---------|------|---------|
| `Failed to fetch` | 后端未启动 | 启动后端服务 `cd backend && npm start` |
| `ERR_CONNECTION_REFUSED` | 端口3001未监听 | 检查后端是否启动，检查端口是否被占用 |
| `405 Method Not Allowed` | 路由配置错误 | 检查后端routes/upload.js |
| `CORS policy` | 跨域问题 | 后端已配置cors，重启后端 |

### 6. 解决favicon.ico 404错误（可选）

在 `frontend/public/` 目录下创建一个空的 `favicon.ico` 文件：

```powershell
cd frontend
New-Item -ItemType Directory -Force -Path public
# 下载一个favicon或创建空文件
echo $null > public/favicon.ico
```

## 快速修复步骤

### 方案A：重启所有服务

```powershell
# 终端1 - 启动后端
cd D:\xiangmu\shejixitong\backend
npm start

# 终端2 - 启动前端
cd D:\xiangmu\shejixitong\frontend  
npm run dev
```

### 方案B：检查端口占用

```powershell
# 检查3001端口是否被占用
netstat -ano | findstr :3001

# 如果被占用，杀死进程（xxxx为进程ID）
taskkill /PID xxxx /F

# 重新启动后端
cd backend
npm start
```

### 方案C：清理并重装依赖

```powershell
# 后端
cd backend
Remove-Item node_modules -Recurse -Force
Remove-Item package-lock.json -Force
npm install
npm start

# 前端（在新终端）
cd frontend
Remove-Item node_modules -Recurse -Force  
Remove-Item package-lock.json -Force
npm install
npm run dev
```

## 测试上传功能

1. 启动后端和前端后
2. 打开浏览器：http://localhost:5173
3. 准备测试CSV文件（参考 `examples/` 目录）
4. 上传文件
5. 查看控制台日志

**测试CSV文件示例**：

采区边界（boundary.csv）:
```csv
x,y
0,0
1000,0
1000,600
0,600
```

钻孔坐标（coordinates.csv）:
```csv
id,x,y
ZK-1,100,100
ZK-2,500,300
```

钻孔数据（data.csv）:
```csv
id,rockHardness,gasContent,coalThickness,groundWater
ZK-1,6.5,4.2,3.8,25
ZK-2,7.2,5.8,4.1,32
```

## 调试技巧

### 查看后端日志

后端终端会显示：
- 接收到的请求
- 解析结果
- 错误信息

### 查看网络请求

F12 -> Network -> 选择失败的请求 -> 查看：
- **Headers**: 请求方法、URL
- **Payload**: 上传的文件
- **Response**: 服务器返回的错误

### 常见成功标志

**后端日志**：
```
成功导入采区边界，共 4 个顶点
成功导入钻孔坐标，共 2 个钻孔
```

**前端界面**：
- 文件名变绿色
- 显示✓图标
- 日志显示"上传成功"

## 仍然无法解决？

请提供以下信息：

1. **浏览器Console完整错误信息**（F12 -> Console）
2. **Network请求详情**（F12 -> Network -> 失败的请求）
3. **后端终端输出**
4. **Node.js版本**：`node --version`
5. **npm版本**：`npm --version`

## 已知问题

### multer中文文件名乱码
- ✅ 已在`upload.js`中处理，使用Buffer转换

### 大文件上传超时
- 限制：10MB
- 如需上传更大文件，修改`upload.js`中的`limits.fileSize`

### Windows路径问题
- ✅ 已使用`path.join`处理跨平台路径
